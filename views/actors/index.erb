<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Basic HTML Page</title>
    <link rel="stylesheet" href="home.css" />
  </head>

  <body>
    <form action="/search_actors" method="get">
    <label for="Search">Search:</label>
    <input type="text" id="search" name="search_results" />
    <input type="submit" />
    </form>
    <table><tr><td>Name</td><td>Number of Movies</td></tr>
    <%@actors.each do |actor|%>
      <tr><td><a href="/actors/<%=actor["id"]%>"><%=actor["name"]%></a></td><td style="text-align:right"><%=actor["movie_count"]%></td></tr>
    <%end%>
    </table>
    <div style="width:25%">
      <% if @page_number != "1" %>
        <a href="/actors?page=<%= @page_number.to_i - 1%>"> Previous </a>
      <% end %>
      <a style="float:right" href="/actors?page=<%= @page_number.to_i + 1%>"> Next </a>
    </div>
  </body>
</html>
